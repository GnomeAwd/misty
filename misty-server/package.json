{
  "name": "hono-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts | bunx pino-pretty",
    "start": "bun run src/server.ts",
    "studio": "bunx drizzle-kit studio",
    "db:generate": "bunx drizzle-kit generate",
    "db:migrate": "bunx drizzle-kit migrate",
    "db:push": "bunx drizzle-kit push"
  },
  "dependencies": {
    "@libsql/client": "^0.15.11",
    "drizzle-orm": "^0.44.4",
    "hono": "^4.6.5",
    "musicbrainz-api": "^0.25.1",
    "pino": "^9.9.0",
    "pino-pretty": "^13.1.1"
  },
  "devDependencies": {
    "@types/bun": "^1.2.20",
    "@types/node": "^24.3.0",
    "drizzle-kit": "^0.31.4"
  }
}
